<div class="back">
  <div class="new">
    <div class='container post-back px-5 pb-5'>
　　　<h3 class='title mb-3'>＃<%= @keyword %>の検索結果</h3>
      <% if @range == 'ユーザー' %>
        <%= render 'public/members/member', members: @members %>
        <%= paginate @members %>
        <% if @members.count == 0 %>
          検索結果が見つかりませんでした。
        <% end %>
      <% elsif @range == '投稿' %>
        <div class='row'>
          <div class='col-10'>
            <div class='posts'>
              <%= render 'public/posts/post', posts: @posts %>
            </div>
            <%= paginate @posts %>
          </div>
        </div>
        <% if @posts.count == 0 %>
          検索結果が見つかりませんでした。
        <% end %>
      <% end %>
    </div>
  </div>
</div>