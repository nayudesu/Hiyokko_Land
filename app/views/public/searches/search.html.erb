<div class="back">
  <div class="new">
    <div class='container post-back'>
　　　<h3 class='title'>＃<%= @keyword %>の検索結果</h3>
    
      <% if @range == 'ユーザー' %>
        <%= render 'admin/members/member', members: @members %>
      <% elsif @range == '投稿' %>
        <%= render 'public/posts/post', posts: @posts %>
      <% else %>
        検索結果が見つかりませんでした。
      <% end %>
    </div>
  </div>
</div>